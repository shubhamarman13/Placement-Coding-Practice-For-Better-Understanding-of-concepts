#include <iostream>
#include<string>
#include<vector>
#include<algorithm>
using namespace std;
int main()
{
   string name = "aab";
   int siz=value.size();
for(int i =0;i<siz;i++)
{
   cout<<value[i]<<" ";
}
vector <string > final;
final.push_back(temp);
final.push_back(name);

}
